sessions/*.md
!sessions/.gitkeep

temp/*
!temp/.gitkeep
sessions/archive/
.consolidated-to

# Regenerated by `horus capabilities --project <name>`; a publishing artifact,
# not a source of truth — committing it would only churn the timestamp stamp.
capabilities.json
