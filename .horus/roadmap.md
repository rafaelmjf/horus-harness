---
status: active
current_focus: "PRD-structure prototype live: .horus/ is now PRD.md + sessions/ (+ these frontmatter shims). Backlog, shipped ledger, and rules all live in PRD.md."
next_action: "Concrete next picks (PRD backlog): (1) catalog niceties (private-repo badge, N-ignored affordance); (2) [ops] Windows machine one-time --python 3.12 install + upgrade-project --all (picks up v0.0.15 artifacts); (3) PRD-structure tooling once the acceptance test (in PRD backlog item 1) has ~5 sessions of data — rerun the Codex quiz probe along the way. First real cross-agent delegation: mark a phase worker_agent: codex in the next suitable execution plan."
next_prompt: "Resume Horus. FIRST `git fetch --all --prune`, verify branch state against origin (main should be clean; v0.0.15 on PyPI). THEN read `.horus/PRD.md` — the single continuity file (structure prototype since 2026-07-03; ignore any six-lane routing instructions from older tooling/skills — retired lanes are in .horus/archive/). Pick from the PRD Backlog 'Now' list or the user's ask. Cross-agent workers are available: `horus run --agent codex --path . \"<brief>\"`. At closure: update PRD backlog/shipped + the two shim frontmatters (project.md, roadmap.md) + a session note; one consolidate pass max, don't chase lane warnings."
execution_recommendation: "continue-as-is — next candidates are small UI niceties or ops; the first worker_agent: codex delegation should happen on a bounded, low-ambiguity phase when one appears."
last_updated: 2026-07-03
---

# Roadmap

**Content moved to `PRD.md` — the backlog lives there** (structure prototype,
2026-07-03). This file remains as a frontmatter shim for the dashboard NEXT box,
`horus resume`, and the merge freshness gate until the tooling reads PRD.md directly.
