---
status: active
current_focus: "Acceptance test rerun DONE: v3 PASSES (closure ~7–12 calls vs v2 26–32; cold probe 5/5 at 5 calls; zero failure flags; result recorded in PRD item 1). Next: teach the tooling the PRD shape."
next_action: "1) PRD item 1 second half — tooling learns PRD+sessions: templates/init, close --check reads PRD frontmatter (drop shims), consolidate→backlog-hygiene, infer/skills, dashboard; then migrate gym-coach + ttrpg via upgrade-project. (failed/rc=0 bug + registry-UTC: FIXED, PR pending merge.) 2) Awaiting Rafa: GM feel-review ttrpg Phases 3+4; keiko phone re-check."
next_prompt: "Resume Horus. FIRST git fetch --all --prune; main clean, v0.0.18 on PyPI. THEN read .horus/PRD.md (single continuity file; ignore six-lane routing from older tooling). The PRD acceptance test PASSED (see PRD item 1 result + session note 2026-07-03-210751) — primary task is now teaching the tooling the PRD shape: templates + init scaffold PRD+sessions; close --check freshness from PRD frontmatter (drop the shims); consolidate becomes backlog-hygiene; infer/skills rewritten; dashboard renders PRD; finally migrate agentic-gym-coach + agentic-ttrpg via upgrade-project. Watch PRD growth (~236/250 lines — delete done items). At closure: update PRD backlog/shipped + the two shim frontmatters + a session note; one consolidate pass max."
execution_recommendation: "plan-execution — ACTIVE: .horus/execution.md drafted 2026-07-03 (6 phases: frontmatter contract → templates/init → consolidate+skills → dashboard → migration engine → migrate controls + quiz gate). Phase 1 is direct supervisor work; 2–4 delegable in parallel after it."
last_updated: 2026-07-03
---

# Roadmap

**Content moved to `PRD.md` — the backlog lives there** (structure prototype,
2026-07-03). This file remains as a frontmatter shim for the dashboard NEXT box,
`horus resume`, and the merge freshness gate until the tooling reads PRD.md directly.
