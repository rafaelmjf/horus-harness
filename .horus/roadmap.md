---
status: active
current_focus: "Worker visibility shipped this session: mascot badge (v0.0.16) + Live-sessions Reviewed ✓ dismiss (v0.0.17). Hub→cross-project Codex delegation proven twice more (gym-coach CF-Access writes, ttrpg Phase 3), supervisor reproduced all gates."
next_action: "Awaiting user checks: gym-coach phone delete (no token) and ttrpg Phase 3 GM feel. Then PRD backlog picks: (1) watcher-window slice of worker visibility (horus tail + run --watch); (2) catalog niceties; (3) [ops] Windows machine --python 3.12 install (trap now confirmed on Linux too); (4) structure-test quiz rerun as v3 sessions accumulate (2 more today)."
next_prompt: "Resume Horus. FIRST `git fetch --all --prune`, verify branch state against origin (main clean; v0.0.17 on PyPI). THEN read `.horus/PRD.md` — the single continuity file (structure prototype since 2026-07-03; ignore six-lane routing from older tooling/skills). Pick from the PRD Backlog 'Now' list or the user's ask. Cross-agent workers: `horus run --agent codex --posture auto-edit --path <proj> \"<brief>\"` — remember the worker sandbox cannot commit or bind sockets; supervisor reproduces gates, commits, pushes. At closure: update PRD backlog/shipped + the two shim frontmatters + a session note; one consolidate pass max."
execution_recommendation: "continue-as-is for Horus-side backlog items (small, single-agent); delegate the next bounded cross-project phase to a codex worker as proven — supervisor owns gates and git."
last_updated: 2026-07-03
---

# Roadmap

**Content moved to `PRD.md` — the backlog lives there** (structure prototype,
2026-07-03). This file remains as a frontmatter shim for the dashboard NEXT box,
`horus resume`, and the merge freshness gate until the tooling reads PRD.md directly.
