---
status: active
current_focus: "PRD-structure prototype live: .horus/ is now PRD.md + sessions/ (+ these frontmatter shims). Backlog, shipped ledger, and rules all live in PRD.md."
next_action: "Live with the PRD structure for a few sessions. Concrete next picks (PRD backlog): (1) tooling support for the PRD structure if it holds up; (2) [bug] integrate() leaves clones on the horus/chore-… branch; (3) catalog niceties (private-repo badge, N-ignored affordance). [ops] Windows machine still owes its one-time --python 3.12 install + upgrade-project --all."
next_prompt: "Resume Horus. FIRST `git fetch --all --prune`, verify branch state against origin (main should be clean; v0.0.13 on PyPI). THEN read `.horus/PRD.md` — the single continuity file (structure prototype since 2026-07-03; ignore any six-lane routing instructions from older tooling/skills — retired lanes are in .horus/archive/). Pick from the PRD Backlog 'Now' list or the user's ask. At closure: update PRD backlog/shipped + the two shim frontmatters (project.md, roadmap.md) + a session note; one consolidate pass max, don't chase lane warnings."
execution_recommendation: "continue-as-is — next candidates are small fixes/UI niceties or the PRD tooling change (judgment-heavy, single-agent shaped)."
last_updated: 2026-07-03
---

# Roadmap

**Content moved to `PRD.md` — the backlog lives there** (structure prototype,
2026-07-03). This file remains as a frontmatter shim for the dashboard NEXT box,
`horus resume`, and the merge freshness gate until the tooling reads PRD.md directly.
