---
status: active
last_updated: 2026-07-02
---

<!-- 2026-06-29: GitHub remote project catalog row updated with incremental pushedAt-skip refresh -->

# Features — capability ledger

Complete **capabilities** (shippable packages), status-tracked. A feature is a
shippable unit of behaviour, not a task — bug fixes, corrections, and chores live
in `roadmap.md` and never appear here. The action points to build a planned or
in-progress feature live in `roadmap.md`; the *why* behind a shipped one is in
`decisions.md` / `history.md`.

Status: **Shipped** · **In progress** · **Planned**

## Shipped

| Capability | Since | Notes |
|---|---|---|
| Stale-build artifact-write guard | 2026-07-02 | PR #63. `selfupdate.build_state()` compares the dashboard's loaded `__version__` to the on-disk dist metadata (re-read per call; stale only when the disk is *newer*, so dev checkouts ahead of their install metadata never warn). When stale, every page banners "restart Horus — this dashboard runs an old build" and the artifact-mutating endpoints (`/upgrade-project`, `/offboard`, `/github-onboard`) refuse with an explanatory banner instead of stamping the old generation. Closes the "stale server self-reports fresh and re-breaks projects" trap from the two-machine test → history.md. |
| Self-update env migration + landing verification | 2026-07-02 | PR #64. The PyPI answer now carries `requires_python`; `run_upgrade` (a) migrates an interpreter-pinned tool env via `uv tool install --force --python <floor>` when the running interpreter is below the latest release's floor, (b) uses `--reinstall` on the plain path (implies `--refresh`; `uv tool upgrade` rejects a bare `--refresh` as of uv 0.11 — caught by driving the real button), and (c) post-verifies the on-disk dist reached the latest release, reporting the one-time migration command instead of a silent old-version "success". README documents the migration. Extends the Dashboard self-update signal row above. |
| `horus upgrade-project --all` | 2026-07-02 | PR #65 (delegated worker phase). Registry-wide projection refresh: iterates every registered project with the same per-project plumbing/flags as the single path; skips registered-but-missing paths (other-machine registry entries), refuses `--all`+explicit `--path` (rc 2), dry-run rc 1 when anything is pending anywhere, one-line summary. Extends the Project upgrade / projection refresh row below. |
| `horus doctor machine` | 2026-07-02 | PR #66 (delegated worker phase). Machine-level checks — the one command when "the app won't open": `horus` console script on PATH (fail), interpreter vs the installed dist's Requires-Python floor (warn + the one-time `--python` migration command), committed hook commands resolvable (fail per broken command, third-party hooks included — any broken hook spams every tool call), Tk present (warn), `gh` auth (warn). Read-only, stdlib-only, included in `doctor all`; only machine *fails* flip the exit code. |
| Read-only session discovery + Recent sessions panel | 2026-07-01 | PR #54 + #55. `horus/session_discovery.py` finds every Claude/Codex session that touched a project from the transcripts the CLIs already write (Claude `~/.claude/projects/**/*.jsonl` by `cwd`; Codex `$CODEX_HOME/sessions/**/rollout-*.jsonl` by `session_meta`/`turn_context`), regardless of how the session was started — the session-visibility path after the Control-cockpit retirement. Counts + timestamps only, never transcript content. Project detail shows an async "Recent sessions" panel (`/project-sessions?i=`, agent/id/last-activity/msgs, max 8). Parsers built by a delegated standard-tier worker; supervisor reproduced the 513-green gate and verified the Claude event-type assumption against a real transcript. action points → roadmap.md |
| Cross-machine closure guard + session archive | 2026-07-01 | PR #53. `horus close --commit --push` fetches first and refuses ("origin has newer continuity commit(s) — pull first") when the upstream is ahead on `.horus/`/`AGENTS.md`/`CLAUDE.md` — the one-person-two-machines guard, mirror image of `horus resume`'s fetch-first pickup; offline/no-upstream errs toward allowing. Distilled session summaries now move to `.horus/sessions/archive/` (gitignored, excluded from `recent_sessions`/the to-distill count) instead of being deleted: consolidate ritual + skill v8 wording + `sessions/archive/` gitignore rule. |
| Dashboard self-update signal | 2026-07-01 | PR #55. `horus/selfupdate.py`: cached PyPI JSON check (6h TTL, offline-silent) vs `__version__`; top-nav shows an async "Update to vX" pill (`/update-check`) only when newer; the button POSTs `/self-update` which runs `uv tool upgrade horus-harness` and banners that Horus must be restarted to load it (no hot reload — auto-respawn deferred to MVP5 lifecycle work → roadmap.md). Complements the artifact-staleness badge (project-vs-CLI) with CLI-vs-PyPI. |
| Companion stale-dashboard replacement | 2026-07-01 | PR #55. Fixes the adopted-orphan bug (a 3-day-old dashboard PID surviving quit/reopen forever): the dashboard exposes `/health` (`app`/`version`/`pid`) and the starting mascot adopts a live 8765 server only when it identifies as a Horus dashboard of the *same* version. A stale-version server is killed by its `/health` pid; a legacy pre-`/health` server that brands itself as Horus is killed via netstat pid lookup (Windows); a foreign server is never touched. |
| CI test gate on the Python floor | 2026-07-02 | `.github/workflows/tests.yml`: pytest matrix on the `requires-python` floor + latest (3.12 + 3.13 since v0.0.7 raised the floor) with a `compileall` import-syntax gate, on every PR and push to main — until then no CI ran the test suite at all. Added with the v0.0.6 hotfix after the two-machine test found v0.0.5 dead-on-import under 3.11 (f-string backslash, PEP 701-only). → history.md |
| PyPI release v0.0.3 | 2026-07-01 | First release carrying the cross-machine flow: published via the existing GitHub-Release → OIDC trusted-publishing workflow (run verified green). |
| Dashboard sumi-e redesign | 2026-06-30 | Implemented the Claude design handoff inside the existing stdlib `http.server` renderer without adding a framework/build step/CDN: sumi-e dark/light tokens, welcome overlay using the packaged mascot, sticky async accounts rail with 46px usage rings, whole-card project grid, calm red NEXT callout, feature mini-ledger, fold-style GitHub/Not tracked catalog sections, two-column project detail with sticky launch sidebar/context/session/manage panels, responsive table containment, and settings form grid. Existing endpoints and constraints remain: `/accounts-panel` async load, `/launch`, account alias/add/remove, `/upgrade-project`, `/offboard`, `/settings`, GitHub catalog rendering, and retired Control redirect behavior. The Windows companion opens the owned app window by default (`horus app --open`; `--tab` is explicit) and process-tree shutdown reaps spawned dashboard/browser children on quit. Browser smoke on app-launched port 8765 verified loaded account rail, 5 project cards, 46px rings, welcome overlay, fold sections, no legacy remote-card markup, no mini-button overflow, and no detail horizontal scroll; 502 tests passed. action points -> roadmap.md |
| Dashboard account management controls | 2026-06-29 | Control -> Accounts now has an Add account form that maps a Claude isolated config dir or Codex home to an alias using the same local `accounts.toml` model as the CLI. Existing account rows include inline alias editing, so generated aliases like `acct-9a7671` can be renamed to friendly labels; renames carry Claude/Codex isolated-dir mappings and, when the dashboard can discover the native account identifier from the configured login home, persist the identifier→alias mapping without rendering the raw identifier in the page. Untracked GitHub cards now state that Onboard uses this machine's `gh` GitHub login for clone/PR and that Claude/Codex account choice happens when launching work. **Account setup wizard 2026-06-29:** the Add-account form no longer asks for a config path — it takes agent + alias, derives an isolated login dir via `config.account_login_dir` (`~/.horus/accounts/<agent>-<alias>`), records the mapping, and the "Add & sign in" button (`/account-login` → `process_account_login`) opens a native terminal running the agent's login (`claude`, or `codex login`) with `CLAUDE_CONFIG_DIR`/`CODEX_HOME` set, so the directory is filled by the sign-in itself (`launcher.login_argv_env`). If the terminal can't open (headless POSIX) the mapping still stands with a `login_error` notice. **Post-Onboard handoff 2026-06-29:** a successful `/github-onboard` now prepends a start-work CTA (`render_onboard_handoff`) with an account-alias chooser + fresh/resume launch for the newly registered project (located via `_project_index`), or a pointer to the account wizard when no account exists. action points → roadmap.md |
| Minimum-context resume handoff | 2026-06-29 | `horus resume [--path]` prints a generated fresh-session handoff that starts with `git fetch --all --prune`, branch verification, compact current state (`current_focus`, `next_action`, `execution_recommendation`, `execution_status`, latest session), and explicit lazy-load guidance for deeper `.horus/` lanes. Dashboard resume cards and `horus start github:<owner>/<repo>` now use the same `routines.resume_prompt()` surface instead of telling agents to read every lane up front. Implemented by a real standard-tier worker in phase 1B and accepted by frontier review. action points → roadmap.md |
| Dashboard NEXT implementation-mode hint | 2026-06-29 | The dashboard NEXT box now turns `roadmap.md` `execution_recommendation` into an explicit "Recommended mode" hint: `plan-execution` shows "Craft execution.md + delegate bounded tasks"; `continue-as-is` shows "Proceed directly with the frontier model"; unknown phrasing asks the planner to choose direct vs delegated. The stored recommendation/rationale remains visible below the hint. action points → roadmap.md |
| GitHub untracked-repo onboarding | 2026-06-29 | PR #37. `discover()` returns `DiscoveryResult(projects, untracked)` — repos without `.horus/project.md` are classified as `UntrackedRepo` (not dropped) and the `pushedAt` cache remembers the not-Horus verdict so unchanged repos skip the `gh api` check. Per-machine `ignored_repos` (`horus ignore [--list]` / `horus unignore` + `github_catalog.filter_ignored()`, opt-out). `horus onboard github:owner/repo` clones into the workspace root if needed → `horus init` → integrates the new `.horus/` via the workflow policy so onboarding never leaves a local-only commit. Dashboard shows a "Not tracked (N)" grid (per-repo Onboard / Ignore) + a collapsed "Hidden (N)" list (Unignore) via owner-validated, same-origin-guarded POST endpoints; 2026-06-29 follow-up made Ignore/Unignore validate trusted `owner/repo` targets and PRG back to `/#github-catalog` so normal browser form submits do not land on raw fragment HTML. Plus a per-machine "no GitHub owner configured" CTA when blank. **Ignore-in-place 2026-07-01 (PR #53):** a delegated submit listener turns Ignore into a fetch POST (`X-Horus-Fetch` → 204) that removes the card without a reload, so several repos can be ignored in one sitting; non-JS keeps the PRG fallback. **Onboard PRG + workspace-clone match 2026-07-02 (v0.0.8, PR #62):** the onboard POST redirects — success lands on the new project's detail page (Start-a-session card = the CTA) with a banner carrying the continuity-PR link + integration detail, failures land on `/` with an error banner (`render_onboard_handoff` deleted as superseded); and `_match_local` probes the conventional clone destination `workspace_root/<name>` (remote-verified) after the registry, so cloned-but-unregistered repos read "cloned, not initialized" instead of "remote only". action points → roadmap.md |
| Dashboard artifact-staleness badge | 2026-06-29 | `load_project` computes, read-only on render, whether a local project's Horus-projected artifacts (managed blocks, skills, hooks) lag the installed CLI — `upgrade.upgrade_project(root, apply=False)` with stale ⇔ any `would-update` action, guarded so the check never breaks the dashboard. The overview card shows a "⚠ artifacts outdated" pill; the project detail page shows the count + `horus upgrade-project --apply`. Remote-only projects aren't checked. A one-click Apply button is a later item. action points → roadmap.md |
| Horus workflow policy + integration helper | 2026-06-29 | PR #37. Configurable `[workflow]` policy in `~/.horus/config.toml` (`integration` ∈ branch-pr-automerge/branch-pr-review/direct-push/local-only, `commit` ∈ auto/manual, `merge` ∈ auto/review) with validated `load_workflow_policy`/`set_workflow_policy` and a `horus workflow [--show | --integration/--commit/--merge]` CLI. `horus/integration.py` `integrate()` runs the policy's git/GitHub flow (stage→commit→push→`gh pr create`→`gh pr merge --auto`, or stop-at-PR / direct-push / local-only) over a single monkeypatchable `_run`. First consumer is `horus onboard`. A dashboard `/settings` page (nav tab + `render_settings`) edits the policy via a `<select>` per key, saved through a same-origin-guarded POST with a PRG redirect (C-full, 2026-06-29). The `close --commit` wiring is still not done. action points → roadmap.md |
| Execution workflow native projection | 2026-06-29 | `horus execution prompt --target claude|codex|generic` prints a supervisor prompt from `roadmap.md` + `execution.md`; `horus execution handoff <phase>` creates a structured `.horus/temp/<phase>.md` worker note without clobbering existing notes; the bundled `horus-execution` skill projects the workflow into both `.claude/skills/` and `.agents/skills/`. The design keeps native Claude/Codex agent configuration user-owned for now: Horus supplies prompts, model-tier vocabulary, and handoff artifacts, then supervisors review worker notes before durable lane updates. **Delegation rubric 2026-06-29:** the implement-vs-delegate decision is now keyed to **volume × ambiguity × runtime** (a decision table in the v4 `horus-execution` skill + `execution.md` template + supervisor prompt, and in the v6 `horus-consolidate` `execution_recommendation` guidance + closure prompt), runtime-neutral with Opus+Sonnet and GPT-5.5 examples. Three **model-independent disciplines** (reproduce-the-gate, bounded committed-and-pushed checkpoint, safety-in-code-not-reviewer) were lifted into the managed instruction block so they apply to every session, and the honest caveat that supervisor review is not a safety guarantee is baked in. action points -> roadmap.md |
| Active execution plan lane | 2026-06-29 | `.horus/execution.md` is now scaffolded as a tracked, fluid active-plan lane for phased implementation workflows: model-tier policy, phase table, supervisor/worker split, worker handoff contract, and review gates. `.horus/temp/` is scaffolded with `.gitkeep` and ignored for fleeting worker/subagent notes; `horus consolidate` reports temp handoff notes that need review/distillation. The dashboard renders the execution plan on project detail pages, and README/managed instructions/routine docs explain that workers write factual handoff notes while the supervisor updates durable lanes. `roadmap.md` frontmatter now includes `execution_recommendation`, so every authored NEXT records whether to continue directly or prepare `execution.md` + worker/subagents; the dashboard surfaces that recommendation and `horus close --check` requires it. action points → roadmap.md |
| Context cache freshness dashboard signal | 2026-06-29 | `horus/cache_status.py` reads local Claude project JSONL and Codex rollout token logs to estimate whether the latest project turn is warm, at cold-cache risk, or expired. The project detail page shows a Context cache section with last-turn age, 60-minute expiry countdown, and aggregate cache-token fields; live session cards show a compact cache line. This is read-only and explicitly local-estimate only: native CLIs own the real server-side prompt cache state, and Horus does not display transcript content. action points → roadmap.md |
| GitHub remote project catalog | 2026-06-28 | `horus discover github <owner> [--save]` uses the authenticated `gh` CLI to find repos with readable `.horus/project.md`, optionally saves the GitHub user/org in local config, and the dashboard renders saved remote Horus projects even when they are not cloned on the current machine. Remote cards show current focus, next action, local-clone match by normalized remote URL, and a `horus start github:<owner>/<repo>` command for remote-only projects. The dashboard renders local projects immediately and loads the GitHub catalog asynchronously from `/github-catalog`, so slow `gh`/network calls no longer block the main page. Saved owners have last-successful snapshots under `~/.horus/github-cache/`: cached cards render immediately with a last-refreshed note, local clone matches are recomputed on read, and background refresh updates the cache while preserving the last snapshot plus latest error on failure. Users can force live discovery via `horus refresh github <owner|--all>` or the dashboard Refresh button; success/error is reported clearly while the last good cache remains available. **Incremental refresh (2026-06-29):** `discover(owner, ..., prior=<cache snapshot>)` skips both `.horus/` `gh api` reads for any repo whose live `pushedAt` matches the cached `pushed_at`, reusing the cached focus/next-action/next-prompt while still recomputing volatile fields (url, branch, pushed_at, local match); `refresh_cache()` builds the prior map from the on-disk cache. Conservative by design — any push re-fetches; cache format unchanged. `horus config workspace-root [PATH]` stores the machine-local clone root (default `~/projects`), and `horus start` clones with `gh repo clone` when needed, refuses unsafe existing destinations, registers the clone, refreshes Horus-managed projections, and prints the repo's stored next prompt/open command. This is the lightweight central-view bridge; live cross-machine observations remain a future proper-app concern. action points → roadmap.md |
| Horus token overhead report | 2026-06-28 | `horus overhead` quantifies the cost of the Horus workflow without API keys or network calls: static prompt footprint for managed blocks, skills, and hook/routine prompts, plus observed local Claude/Codex token usage for Horus-related turns. Codex reads rollout `token_count` events; Claude reads project JSONL usage and de-duplicates request ids. `--sessions` joins the local registry to native logs by session id for per tracked-session totals where a matching id exists. `--baseline` prints the strict controlled A/B recipe and, when given explicit `--without-horus`/`--with-horus` native session ids, reports matched sessions, turns, token totals, and incremental delta aggregate-only; separate clean-clone paths are supported. The dashboard project detail page surfaces the upper-bound aggregate report. Transcript content stays private. action points → roadmap.md |
| Project upgrade / projection refresh | 2026-06-28 | `horus upgrade-project` is the official per-project refresh path after `uv tool upgrade horus-harness`: dry-run/report by default, `--apply` refreshes Horus-managed AGENTS/CLAUDE blocks, Claude/Codex project skills, and native hooks from the installed CLI. It skips instruction files without a Horus managed block and unversioned skills instead of clobbering unowned content. `horus doctor` now points stale projected artifacts at this command; README documents init + refresh workflows. **Direction-aware for the managed block (2026-07-01, v0.0.4):** the block carries a `horus-block-version` marker; a block *newer* than the installed CLI is skipped with "upgrade horus-harness instead of refreshing" (never a silent downgrade); unmarked legacy blocks count as older and refresh normally. Hook configs remain content-compared (residual → roadmap.md). **Repo-ahead remedy in place (v0.0.5):** that state renders a "Horus CLI outdated" card badge + a detail-page "Update horus-harness from PyPI" button (POST `/self-update`), so the correct fix is one click instead of hunting for the nav pill. **One-click upgrade 2026-06-29:** a same-origin POST `/upgrade-project` (apply by project index) drives `upgrade_project(apply=True)`; the dashboard shows a "Refresh now" button on the project detail page's artifacts-outdated card and a "refresh" button on the overview-card pill, with a result banner. action points → roadmap.md |
| Project offboarding | 2026-06-29 | `horus/offboard.py` + `horus offboard [--apply] [--purge]` is the inverse of init/upgrade: removes the projected artifacts (AGENTS/CLAUDE managed block via the new `instructions.remove_block`, the bundled skills under `.claude`/`.agents`, and Claude/Codex hooks via surgical `native_hooks` removal that keeps non-Horus hooks) and unregisters the project. **Keeps `.horus/` by default** (the durable committed memory); `--purge` also deletes it. Dry-run is the default; `--apply` performs it. Dashboard: an offload control on **both** the project detail page ("Manage Horus integration" card) and each **overview project card** (a `<details>` "Offload project" reveal at the end of the card), offering two explicit choices — "Keep files" (remove integration, keep `.horus/`) and "Remove completely" (red, purges `.horus/`), each a same-origin POST `/offboard` (project-by-index) with its own confirm, redirecting to the overview with a result banner. Action buttons are colour-coded: green (`btn-go`) for refresh/upgrade, red (`btn-danger`) for remove-completely, neutral (`btn-keep`) for keep-files. action points → roadmap.md |
| Horus companion/mascot app | 2026-06-25 | `horus app` / `horus mascot`; borderless always-on-top Tkinter mascot, packaged PNG frames, idle blink/bob motion, starts/detects dashboard + opens it on click, context menu includes close check + quit; launches windowless on Windows (re-exec under `pythonw.exe`, `--no-detach` opt-out) so no console lingers. **Art refreshed 2026-06-26** (new Horus-falcon mascot + a head/bust `icon.png`/`icon.ico`). **Mascot background polished 2026-06-28:** runtime frames are transparent foreground mascot frames again; `--mascot-style auto` defaults to foreground-only on Windows and to a two-layer fixed Egypt background + animated foreground on Linux/other desktops, with explicit `foreground` / `layered` overrides. The lightweight companion now renders at a compact 120px target height, about two-thirds of the prior window size. `background_egypt.png`, `mascot_with_background.png`, and `mascot_without_background.png` stay packaged as source material for future customization. **Lifecycle hardened 2026-06-28:** duplicate dashboard servers refuse to bind the same port, the mascot reuses a live dashboard before spawning, and any dashboard child it owns is terminated, waited on, and killed if necessary when the mascot exits. **Browser shell adjusted 2026-06-28:** lightweight `horus app --open` now opens a normal browser tab by default for clearer OS/taskbar identity; `--app-window` keeps the old Chrome/Edge `--app` mode as an explicit opt-in. **Owned dashboard window + reuse 2026-06-29:** the mascot can open the dashboard as a *dedicated* Chromium app window (`--user-data-dir` under `~/.horus/dashboard-profile` = Horus's own trackable instance); clicking the mascot again raises that existing window (`launcher.focus_window_for_pid`) instead of stacking another tab, and quitting the mascot closes the owned window too. `companion.resolve_open_mode` makes owned the default only where the raise is reliable (Windows); other desktops keep the plain-tab default (no dependable cross-OS raise — Wayland has no API). `horus app --tab` forces a browser tab anywhere; `--app-window` forces owned anywhere. The remaining close-window/taskbar identity gap for non-owned modes belongs to the proper native app (Planned). action points → roadmap.md |
| `horus init` — scaffold `.horus/` + managed instruction blocks | — | never clobbers; injects managed block into existing AGENTS/CLAUDE |
| `horus doctor` (project + instructions) | — | continuity health + cross-ref-normalized drift check |
| `horus dashboard` — read-only multi-project view | — | next-step banner, roadmap breakdown, sessions |
| `horus session new` | — | session summary from template |
| `horus close` (git-aware) | — | verify-first closure ritual; `--commit [--push]` |
| `horus reconcile instructions` | — | deterministic within-repo managed-block sync |
| `horus forget` / `horus prune` | — | drop stale registered projects |
| PyPI distribution via Trusted Publishing | — | `uv build`/`uv publish`, OIDC, no stored token |
| `.horus/` structure v2 (6 lanes: + `features.md` + `history.md`) | — | templates, README, managed block, dashboard rendering, GFM tables in `markdown.py` |
| `horus consolidate` (agent-delegated) | — | pre-pass: roadmap↔features overlap, done-but-unshipped, sessions-to-distill + emitted routing ritual; `docs/routines.md` |
| `horus distill-history` (agent-delegated) | — | source-log detection + size signals + emitted compression ritual; `docs/routines.md` |
| Agent-skills layer (`horus-consolidate` / `-distill-history` / `-infer`) | — | bundled in `horus/skills.py`; `init` scaffolds `.claude/skills/` + `.agents/skills/`; `horus skill install`, version markers, doctor check + nudge; in-app context-aware counterparts to the CLI routines |
| `horus infer` (agent-delegated) | — | discover canonical docs + detect placeholder lanes + emit bootstrap ritual; backs the `horus-infer` skill (replaces the removed deterministic infer) |
| Codex usage/rollover closure signal | — | `horus close` + dashboard read local Codex rollout `token_count` events; warn at 90% by default before another large turn |
| Codex project-skill projection | — | `horus init` / `horus skill install --target codex` write bundled Horus skills to `.agents/skills/` alongside Claude `.claude/skills/` |
| Codex native usage hook | 2026-06-25 | `horus usage check` plus `horus hook install --target codex`; writes project `.codex/hooks.json` `UserPromptSubmit` + `Stop` hooks. At threshold, hook mode emits Codex-compatible JSON: pre-task `additionalContext` (advisory) and `decision:block` on `Stop`. 2026-06-29 the injected text was made non-coercive — UserPromptSubmit defers to the user's explicit request (incl. push) and only offers closure; Stop *asks* the user (close now vs push ahead) instead of force-closing; both always use `horus close --commit --push`. Validated against Codex CLI 0.142.2 hook semantics on 2026-06-27; Codex may still require `/hooks` trust review before the project hook runs |
| Codex pre-merge closure gate | 2026-06-27 | `horus hook install --target codex --kind merge` writes a `.codex/hooks.json` `PreToolUse`/`Bash` hook running `horus close --hook`. On `gh pr merge ...`, it runs the same freshness gate as Claude: stale lanes -> structured deny + `horus-consolidate` diversion; fresh lanes -> silent allow. This was not fundamental unsupported; Horus's previous "merge is Claude-only" rule was stale relative to current Codex hooks |
| Claude usage→closure hook | 2026-06-25 | `horus/claude_usage.py` reads 5h/weekly % from the OAuth `/usage` endpoint; `horus usage check --target claude`; `horus hook install --target claude` writes `.claude/settings.json` `UserPromptSubmit` + `Stop` hooks that fire at threshold (re-armable per session). 2026-06-29 made the injection non-coercive: UserPromptSubmit injects advisory `additionalContext` that defers to the user's explicit request (never a continuity-only override); Stop `decision:block` *asks* the user (close now vs push ahead) rather than force-closing; both always use `horus close --commit --push` so closure reaches the remote |
| Aliased account-tagged sessions | 2026-06-25 | `horus session new` records *which* account ran a session via a local alias (`config.alias_for` + `~/.horus/accounts.toml`, `acct-<sha6>` fallback); `horus account [--set]`; the real email never lands in a commit |
| Account usage + launch on Projects tab | 2026-06-30 | After retiring the Control cockpit, the useful bits live on the Projects tab: an **async accounts/usage strip** on the index (`_accounts_strip` via `data-horus-src='/accounts-panel'` — usage rings + weekly bar + alias editing + add-account wizard; loads off the OAuth `/usage` call so it never blocks the page) and a **"Start a session"** card on each project detail page (`_project_launch_form`, now native-window-only + copy-command; work-pickup model) with an **agent picker (Claude Code / Codex)** so the button spawns either in a real OS terminal (`launch_interactive` is agent-agnostic; the in-app Python PTY target was retired). Launch/account POSTs redirect to `/` or `/project?i=`; unified `_notice` banner. **Quick-launch + remove (2026-06-30):** each account row has a **"+ session"** button (fresh native-terminal session as that account, its agent, home dir) and a red **remove** button (`config.remove_account` unmaps the account from `accounts.toml` without deleting the on-disk login dir; POST `/account-remove`); each overview project card has a collapsed **"▶ Start a session"** disclosure with the same agent/account/posture/fresh-resume launch form. → roadmap.md |
| ~~Dashboard Control tab~~ (RETIRED 2026-06-30) | 2026-06-26 | **Retired** — the session cockpit overlaps the orchestration surface ceded to Omnigent and only ever tracked Horus-launched sessions. `/control` 302-redirects to `/`; nav link + "● N live" badge removed; `render_control`/`_terminal_panel`/`pty_host`/`/pty/*` left dormant pending deletion. See decisions.md 2026-06-30. Original: `/control` route + header nav (Projects ↔ Control). Control-panel layout from the design mockup: **accounts column** with live usage **donut rings** per Horus-known account (`gather_accounts` reads the `accounts.toml` config-dir map + ambient login; alias-only, best-effort OAuth `/usage`); **projects column** with a launch ▶ that reveals copyable real `horus open "<path>" [--account <alias>]` commands; **live-session cards** (running processes only, from the registry) showing status/agent/pid + the account's 5h usage bar and, for Codex sessions, context window. Read-only, stdlib-only, no JS backend beyond clipboard copy. Header **"● N live" indicator** (count of `running` sessions, on every page, links to Control) + per-card jump-to-session shortcuts (`horus focus <id>` to raise the running window; `claude --resume <id>` for a new view) |
| `horus focus <session_id>` | 2026-06-26 | Raises a running session's terminal window by PID (`launcher.focus_window_for_pid`: Toolhelp descendant-tree match + `EnumWindows` + `SetForegroundWindow`; git-style id-prefix lookup). The true "open the native app" the read-only dashboard can't do. Windows-only, best-effort (foreground lock; shared-Terminal hosting may miss → clear fallback message) |
| Dashboard launch buttons (first POST surface) | 2026-06-26 | The Control tab *launches* sessions, not just shows commands (PR #11). `POST /launch` via shared `horus/launch.py` (used by `horus open` too); same-origin-guarded, loopback-only, projects-by-index, accounts validated. Account "+ session" (fresh) + project play (Fresh / Resume-with-continuity-prompt) + a **permission-posture select** (Ask / Plan / Accept-edits / Bypass → Claude `--permission-mode`, changeable later in the TUI). action points → roadmap.md (MVP4) |
| Cross-platform PTY (`pty_session.py`) | 2026-06-26 | One byte-oriented pseudo-terminal handle: `pywinpty`/ConPTY on Windows (conditional dep), stdlib `pty` on macOS/Linux (dep-free). read/write/resize/isalive/terminate |
| In-app terminal: PTY session-host + xterm.js | 2026-06-26 | The real `claude`/`codex` TUI runs under a PTY in a persistent **session-host** (`pty_host.py`) and renders with vendored xterm.js (local, no CDN) inside the dashboard. Bytes stream over SSE (`/pty/stream`); keystrokes/resize POST back (`/pty/input`,`/pty/resize`,`/pty/kill`). Sessions persist independent of viewers → **re-attach** across tabs/reloads; **multi-viewer**. action points → roadmap.md (MVP4) |
| Terminal pop-out window | 2026-06-26 | `⤢` opens a host-owned session in its own browser window (`/pty/term`) — a second viewer of the same persistent PTY. The practical "drag out"; "drag in" = the session stays a tab to re-attach. Only for Horus-started sessions |
| Closure freshness gate (`horus close --check`) | 2026-06-26 | Deterministic detection (`routines.freshness_signals`, never authoring) that the dashboard's read-surface is current with this session: `project.md`/`roadmap.md` `last_updated` vs newest session; empty `next_action`/`next_prompt`/`current_focus`; `next_action` matching already-shipped work. `--check` exits non-zero while stale, so closure isn't "done" until the dashboard is fresh. action points → roadmap.md |
| Continuity PR check (CI) | 2026-06-26 | `.github/workflows/continuity.yml` — advisory pre-merge gate running `horus close --check` + a git heuristic ("code changed but no lane updated"); annotates, doesn't block (sessions gitignored → not on CI). The PR-boundary closure nudge |
| Local pre-merge closure gate | 2026-06-26 | `horus close --hook` — a Claude **PreToolUse** hook that intercepts `gh pr merge` *in the session*: ignores non-merge tool calls, runs `closure.freshness_gate` on a merge, and emits `permissionDecision:deny` (block + divert to horus-consolidate) when lanes are stale, else allows. The real in-loop gate the CI check couldn't be (CI is server-side/post-push/advisory/sessions-blind). Installed via `horus hook install --kind {usage,merge,all}` (default `usage`; `merge` is Claude-only); `_merge_event_hook` generalized so usage + merge hooks coexist. Self-clearing (consolidate → `horus close --check` passes → re-run merge). → roadmap.md |
| Codex accounts in the dashboard | 2026-06-26 | Codex accounts surface in the Accounts panel like Claude. `codex_usage.current_account()` reads `tokens.account_id` from `$CODEX_HOME/auth.json`; `latest_account_usage()` returns the newest account-global 5h/weekly rate-limit snapshot across rollouts (no live API → **last-observed**). `gather_accounts` emits Codex rows (isolated `CODEX_HOME` map + ambient `~/.codex`); the usage ring **and a full-width weekly bar with reset date** populate from rollout `rate_limits` as **used%** (matches the Claude ring; inverse of the Codex app's "remaining"). Per-account agent badge, agent-aware launch form (`process_launch` routes `agent=codex` through the PTY host like Claude), `horus account --agent codex [--set-codex-home]`. Bars thinned 14→8px. → roadmap.md |
| Hosted-session self-restart guard | 2026-06-26 | Stops an in-app agent from killing its own host (history.md footgun). `pty_host.start` injects `HORUS_HOSTED_SESSION=1` + `HORUS_PTY_HOST_PID` into the PTY env; `horus guard-host --hook` (a Claude **PreToolUse**/`Bash` gate) inherits them and, **only inside a hosted session**, emits `permissionDecision:deny` (`templates.HOSTED_RESTART_INSTRUCTION`) for a command that would restart/kill the host — relaunch (`horus app`/`dashboard`), kill of the host PID, or kill-by-name/interpreter (`python`/`horus`/`dashboard`). No-op outside a hosted session (normal terminals untouched); errs toward allowing. Installed via `horus hook install --target claude --kind {guard,all}` (`install_claude_guard_hook`; Claude-only, coexists with usage + merge as a second PreToolUse/Bash group). Pure env+string logic, no OS-divergent process APIs → most cross-OS option (vs the deferred daemon). Dogfooded into this repo. → roadmap.md (MVP5) |
| Codex hosted-session guard | 2026-06-27 | Codex parity for the self-kill guard: `horus hook install --target codex --kind guard` writes a project `.codex/hooks.json` `PreToolUse`/`Bash` hook running `horus guard-host --hook` with `commandWindows` support. It uses the same `HORUS_HOSTED_SESSION` + `HORUS_PTY_HOST_PID` env markers injected by the PTY host and the same structured deny response, so an in-app Codex session launched via Horus cannot restart/kill the dashboard host. Coexists with Codex usage + merge hooks; requires Codex `/hooks` trust review after changes |
| Codex adapter | 2026-06-26 | `horus/adapters/codex.py` (`CodexAdapter`): all four contract methods + `interactive_command`. Spawn: `codex exec --json [-m model] [sandbox-flags] <prompt>`; resume: `codex exec resume --json <session_id> [prompt]` (exec resume has no `--sandbox`; only FULL_AUTO bypass forwarded). `CODEX_HOME` per-account isolation via `[codex_homes]` in `~/.horus/accounts.toml` (`config.load_account_codex_homes()`). JSONL event parsing: `thread.started` → SESSION_STARTED (`thread_id`); `item.completed/agent_message` → ASSISTANT_TEXT; `tool_call/tool_output/approval_request` → TOOL_USE/TOOL_RESULT/PERMISSION_REQUEST; `turn.completed` → RESULT. `interactive_command` runs bare `codex [flags] [prompt]` under the PTY host; Codex has no `--session-id` pre-assignment so session_id is Horus's internal `term_id`. 23 tests; live proof confirmed (`session_started → assistant_text → result`). `get_adapter("codex")` resolves; PTY host and dashboard launch buttons work unchanged. Vendor-neutral contract proven at N=2. → roadmap.md |

## In progress

| Capability | Notes |
|---|---|
| Proper native desktop app (window lifecycle, tray, icon) | **Planned, separate downloadable package.** The lightweight tier (Edge `--app` + mascot, shipped via uv) can't own the window lifecycle (close-window→quit, no stale tab) — that needs a real app shell. Stack TBD (PySide6 / Electron / Tauri — trade-offs in decisions.md "pywebview Tried and Rejected"). Shares the same Python server + web UI (the stable contract), so it's an additive host, not a rewrite. NB: pywebview was tried for this and **rejected** (unstable + slow on Win11 — history.md). → roadmap.md (MVP5) |
| Agent execution layer (MVP3→4) | shipped: adapter contract + `FakeAdapter` + `ClaudeAdapter` + **`CodexAdapter`** (vendor-neutral contract proven at N=2), session/process registry, multi-account isolation (Claude `CLAUDE_CONFIG_DIR` + Codex `CODEX_HOME`), `horus run` + `horus open` + in-app PTY terminal. Now genuinely multi-agent. Still open: oversight terminate/resume from UI + autonomous closure. → roadmap.md |
| Routine + skill validation on a real project | invoke on fabric in a CLI-equipped session; tune skill triggering (`claude -p`); harmonize siblings → `roadmap.md` |

## Planned

| Capability | Notes |
|---|---|
| rulesync projection to other tools | Phase 3; evaluate for broader AGENTS/CLAUDE/instruction sync beyond Horus's direct Claude/Codex skill and hook projection |
| Companion status signals | Build on the mascot: usage warning, stale session summary, uncommitted continuity, hook active/trusted state, dashboard server state |
| Autonomous routine variant (Horus spawns the agent) | the spawning half of consolidate/distill; MVP3 (now unblocked — adapters exist) |
| LLM-based `horus infer` | distill `.horus/` from canonical docs; lands with MVP3 |
