# Product audit — 2026-07-16 (horus 0.0.58)

First real audit; the `0.0.57 2026-07-16` stamp was initial (set when the skill
shipped in PR #258, no verdicts behind it), so the window was the whole live
surface. Evidence: git history, `.horus/` artifacts, integration-point grep
(managed blocks, native_hooks, TUI, dashboard, skills, scripts), owner
interview. Owner's actual workflow: TUI launch/resume of projects, incl. mobile.

| Finding | Verdict | Evidence |
| --- | --- | --- |
| `horus brainstorm` | retire (proposed) | zero usage, zero references; Claude Code plan mode covers it |
| `horus vscode-task` | retire (proposed) | zero usage, untouched since 2026-07-02; owner entry points are TUI + mobile |
| `horus overhead` | retire (proposed) | zero references anywhere; measurement one-off |
| Companion `app`/`mascot` | retire (proposed) | zero usage; TUI + dashboard are the cockpit; duplicate spellings |
| `horus focus` | defer | Windows window-raise still wired into dashboard; owned-window model mostly superseded by managed tmux — recheck next audit |
| Closure-commit frequency | defer | ~8 closure commits of 16 on 2026-07-16 looked heavy, but owner didn't flag it and handoff granularity was one day old — recheck with a real window |
| Native memory/orchestration overlap | no-change | repo-local committed `.horus/` stays cross-model and machine-portable vs account-local native memory; Horus deliberately doesn't compete with native orchestration |
| Substrate verbs (`attach`/`stop`/`reap`/`open`/`discover`/…) | no-change | plumbing under TUI/dashboard flows the owner uses |

Owner decision 2026-07-16: retire proposals left un-carded for now (skill-test
session). Anti-ceremony guard: not applicable (retire proposals present; prior
audit was an initial stamp).
